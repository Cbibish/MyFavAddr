meta {
  name: Get public addresses
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/api/addresses/public
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("returns items array", function() {
    expect(res.getBody()).to.have.property("items");
  });
}